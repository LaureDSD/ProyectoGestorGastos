Necesitas tenre instalado el JDK Amazon Coretto 22.0.2 y Lombok
Las dependecias se instalan desde archivo pom.xml




En el proeycto en el inicializador se encuentra las credenciales de acceso para admin, se puede crear usuarios default.

RUTAS:

localhost:8080/swagger
localhost:8080/auth/login cceso a dashbaord(Correo y contrasena)

"admin@gesthor.com" + "admin"

"user@gesthor.com" + "user"


USO DE FUNCIONES DE GOOGLE Y GITHUB

Agregar clabe de usuario de google y key , igual para git en el archivo properties.

La clave de la api debe ser igual en el server python y spring

Configua las rutas y demas si es necesario.

La key de la api de OpenAi NO SE AGREGA, por motivos de seguridad. Sirve culaquiera que permita GPT 3.5 Turbo

LINUX Y MAC

export GITHUB_CLIENT_ID="tu_client_id_github"
export GITHUB_CLIENT_SECRET="tu_client_secret_github"
export GOOGLE_CLIENT_ID="tu_client_id_google"
export GOOGLE_CLIENT_SECRET="tu_client_secret_google"

WINDOWS

set GITHUB_CLIENT_ID=tu_client_id_github
set GITHUB_CLIENT_SECRET=tu_client_secret_github
set GOOGLE_CLIENT_ID=tu_client_id_google
set GOOGLE_CLIENT_SECRET=tu_client_secret_google